# Daymark product plan

> Historical Phase 0–2 notes that referenced an external bookmark fork have been
> removed as part of Epic 0 (standalone Daymark). Current stack: `apps/daymark`
> (Vite client) + `apps/api` (Hono sync API).

## Direction

- Daily memory resurfacing (Memories) as the core ritual
- Library + capture + import for saves
- On-device enrichment and constellations (later epics)
- Cloud sync via `apps/api` (auth, saves, surfacing state)

See Linear team **MARK** for epics MARK-1 onward.
